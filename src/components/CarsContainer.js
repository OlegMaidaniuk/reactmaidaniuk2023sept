import {useEffect, useState} from "react";

const CarsContainer = () => {
    const [cars, setCars] = useState([]);
  useEffect(()=>{},[]);
    return (
        <div>
            CarsContainer
        </div>
    );
};

export {CarsContainer};